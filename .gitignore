# Build artifacts
/target
*.so
*.dylib
*.dll

# Cargo
/target/
tests/server/target/
tests/server/Cargo.lock
**/*.rs.bk

# Local debug scripts (not part of the test suite)
tests/test_firestore.php
tests/test_firestore_*.php
!tests/test_firestore_fake.php
tests/test_*_debug.php
tests/test_with_roots.php
tests/Dockerfile.test

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# IDE / Editors
.idea/
.vscode/
*.swp
*.swo
*~
.project
.settings/
*.sublime-workspace
*.sublime-project

# Environment / secrets
.env
.env.*

# Claude Code
.claude/

# Docker (not needed in repo, .dockerignore handles build context)
# But ignore any local docker-compose overrides
docker-compose.override.yml

# PHP (vendor installed inside Docker only)
/vendor/
composer.lock

# OS
Thumbs.db
ehthumbs.db
Desktop.ini
